import React from 'react';

const Modal = (props) => {
  // eslint-disable-next-line no-unused-vars
  const { player, togglePlacementFlag, placeShip } = props;
  return (
    <div className="modal">
      This is a modal!
    </div>
  );
};

export default Modal;
